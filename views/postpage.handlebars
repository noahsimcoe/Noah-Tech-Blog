<div>
    <h2>{{title}}</h2>
    <h4>{{description}}</h4>
    <h6>Created by {{user.username}} on {{format_date time}}</h6>
    <br>
    <h3>Comments:</h3>
    {{#each comments as |comment| }}
        <h3>{{comment.description}}</h3>
        <h5>-{{comment.user.username}}</h5>
    {{/each}}
</div>